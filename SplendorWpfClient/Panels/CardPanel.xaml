<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ValueConverters="clr-namespace:SplendorWpfClient.ValueConverters" xmlns:ViewModels="clr-namespace:SplendorWpfClient.ViewModels" x:Class="SplendorWpfClient.Panels.CardPanel" 
             mc:Ignorable="d" 
             d:DesignHeight="262" Width="218">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <ViewModels:CardViewModel/>
    </UserControl.DataContext>



    <Border x:Name="Background" BorderBrush="Black" BorderThickness="2" CornerRadius="10">
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="0"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid Margin="10,97,156,10">
                <ListView ItemsSource="{Binding VisibleCost}" Background="{x:Null}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="CostBorder" HorizontalAlignment="Left" Margin="10,0,0,10" VerticalAlignment="Bottom" Width="20" BorderThickness="1" BorderBrush="Black" CornerRadius="14" Background="{Binding Color, Converter={StaticResource ColorToBackground}}" >
                                <Label x:Name="Cost" Content="{Binding Value}" RenderTransformOrigin="0.567,0.192" Height="20" HorizontalContentAlignment="Center" FontSize="15" FontWeight="Bold" VerticalContentAlignment="Center" Padding="0"/>
                            </Border>
                        </DataTemplate>
                    
                    </ListView.ItemTemplate>
                
                </ListView>
            </Grid>
            <Label Content="{Binding VictoryPoints}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="82" Width="214" Visibility="{Binding IsVictoryPointsPresent, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"/>
        </Grid>
    </Border>
</UserControl>
